apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: dev
  labels:
    app: backend
    app: frontend
# Add non-sensitive environment variables here
# Example:
# data:
#   NODE_ENV: "production"
